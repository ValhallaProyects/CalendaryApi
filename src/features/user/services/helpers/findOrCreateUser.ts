const findOrCreateUser = async (email) => {
  // const rec = await findUser({where:{email}})
  // const rec = await findAuth({ where: { email } });
  // if rec === false
  //     Create User & Auth
  // else
  //     Return rec
  // if (!rec) {
  // }
  // return rec;
};

export { findOrCreateUser };
